#include <stdlib.h>

int main(void)
{
    system ("dir");	        // Wyswietlenie zawartosci aktualnego katalogu
    system("pause");
    system("cls");          //czysci ekran
    system ("PAUSE");       // Wstrzymuje prace programu i czeka na wcisniecie klawisza
    system("path");         //Wyswiela dostepne sciezki
    system("pause");
    system ("dir *.exe");   //Wyswietlenie zawartosci aktualnego katalogu dla plików z rozszerzeniem exe
    return 0;
}
